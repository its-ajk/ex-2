name: Issue Listener
on: [push,issues]
jobs:
  Print_Issue_Details:
    runs-on: ubuntu-latest
    steps:
      - name: print json
        run: echo "${{toJSON(github)}}""
      - name: print raw
        run: echo "${{github}}"
